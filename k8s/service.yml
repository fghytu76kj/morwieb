apiVersion: v1
kind: Service
metadata:
  name: grdesa
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "grdesa"
    port: 8080
  selector:
    app: grdesa
