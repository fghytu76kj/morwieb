apiVersion: v1
kind: Service
metadata:
  name: sdres
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sdres"
    port: 8080
  selector:
    app: sdres
